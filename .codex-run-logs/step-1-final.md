現在の実行環境が `read-only` のため、`codex-plan.md` を作成できませんでした。プロジェクト構成は確認済みで、`Assets` フォルダは存在しています。

`codex-plan.md` に書く内容は以下です。

```md
# Codex 実行計画

## 目的
Unity プロジェクトの `Assets` フォルダ直下に `CodexTest.cs` を追加する。

## 作成予定ファイル
`Assets/CodexTest.cs`

## ファイル内容
```csharp
//これはテストです！
```

## 実施手順
1. `Assets` フォルダが存在することを確認する。
2. `Assets/CodexTest.cs` を新規作成する。
3. ファイル内容を `//これはテストです！` のみにする。
4. 作成後、ファイルの存在と内容を確認する。
```

現時点では `CodexTest.cs` も `codex-plan.md` も変更していません。