
# Codex Plan

## Step 1

1. `Assets` フォルダ直下に `CodexTest.cs` が存在するか確認する。
2. `Assets/CodexTest.cs` の現在の内容を確認する。
3. 次の実行ステップで、内容を `//これはテストです（Ver2!）` の 1 行に変更する。
4. 変更後、配置場所と内容を再確認する。
5. ただし、この計画作成ステップでは `Assets/CodexTest.cs` は変更しない。

## Current Notes

- 現時点では `Assets/CodexTest.cs` は変更しない。
- 確認時点で `Assets/CodexTest.cs` は既に存在する。
- 確認時点で `Assets/CodexTest.cs` の内容は既に `//これはテストです（Ver2!）` だった。
