
# Codex Plan

## Step 1

1. `Assets` フォルダ直下に `CodexTest.cs` が存在するか確認する。
2. 現在の `Assets/CodexTest.cs` の内容を確認する。
3. 次の作業ステップで、ファイル内容を `//これはテストです（Ver2!）` の 1 行に更新する。
4. 更新後、`Assets/CodexTest.cs` の配置場所と内容を再確認する。

## Current Notes

- 現時点では `Assets/CodexTest.cs` は変更しない。
- 確認時点で `Assets/CodexTest.cs` は既に存在し、内容は `//これはテストです！` だった。
- 次回の変更対象内容は `//これはテストです（Ver2!）`。
