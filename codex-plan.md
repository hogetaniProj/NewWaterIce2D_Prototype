
# Codex Plan

## Step 1

1. `Assets` フォルダ直下に `CodexTest.cs` が存在するか確認する。
2. 存在しない場合は `Assets/CodexTest.cs` を作成する。
3. ファイル内容が `//これはテストです！` の 1 行になっているか確認する。
4. 内容が異なる場合のみ、`//これはテストです！` に更新する。
5. 作成または更新後、配置場所と内容を再確認する。

## Current Notes

- 現時点では `Assets/CodexTest.cs` は変更しない。
- 確認時点で `Assets/CodexTest.cs` は既に存在し、内容も `//これはテストです！` だった。
